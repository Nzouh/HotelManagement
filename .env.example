# Example .env file for connecting to the PostgreSQL database

# Replace these with your local PostgreSQL credentials
DB_USER=your_postgres_username
DB_HOST=localhost
DB_NAME=hotel_db
DB_PASSWORD=your_postgres_password_if_any
DB_PORT=5432